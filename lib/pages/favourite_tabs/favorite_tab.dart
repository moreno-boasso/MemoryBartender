// preferiti_tab.dart
import 'package:flutter/material.dart';

class PreferitiTab extends StatelessWidget {
  const PreferitiTab({super.key});

  @override
  Widget build(BuildContext context) {
    // Sostituisci con il contenuto dei cocktail creati dall'utente
    return const Stack(
        children: [
        ]
    );
  }
}